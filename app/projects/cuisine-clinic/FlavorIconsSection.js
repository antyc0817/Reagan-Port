"use client";

import styles from "../project.module.css";

export default function FlavorIconsSection() {
  return (
    <section className={`${styles.projectSection} ${styles.flavorIconsSection}`}>
      <h3 className={styles.projectSectionTitle}>[ Flavor icons ]</h3>
    </section>
  );
}
